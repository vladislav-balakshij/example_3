include ../bem/index
include ../helpers/config
include ../helpers/mixins
- var telephones = ['+38 (063) 550 03 20', '+38 (066) 538 27 00', '+38 (066) 538 27 00', '+4 (879) 045 79 50']
- var emails = ['info@osvitamarket.com'];
+b.SECTION.contact
	+quadro('_style-1 contact__quadro_2')
	+e.map#map
	+e.info
		+e.P.info-title
			+b.SPAN.color-brand-2 Звоните
			|, 
			br
			|приходите в гости
		+e.info-part._phone
			for tel in telephones
				+e.A.info-link(href="tel: "+tel+" ")=tel
		+e.info-part._email
			for email in emails
				+e.A.info-link(href="mailto: "+email+" ")=email

script(async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC4vqrgTZ81rE5juLYbXT6Tg7y6T8Ii0Nc&callback=initMap")
script.
	// This example displays a marker at the center of Australia.
	// When the user clicks the marker, an info window opens.

	function initMap() {
		var uluru = {lat: 50.434675, lng: 30.514878};
		var map = new google.maps.Map(document.getElementById('map'), {
			zoom: 15,
			center: uluru,
			disableDefaultUI: true,
			scrollwheel: false
		});
		
		var title = 'г. Киев, ул. Антоновича, 33 В, оф. 11';

		var contentString = '<div id="content">'+ title + '</div>';

		var infowindow = new google.maps.InfoWindow({
			content: contentString
		});

		var marker = new google.maps.Marker({
			position: uluru,
			map: map,
			title: title,
			icon: 'img/map-marker.png'
		});

		infowindow.open(map, marker);
		//- marker.addListener('click', function() {
			
		//- });
	}